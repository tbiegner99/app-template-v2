meta {
  name: Enable User
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/auth/users/:id/enable
  body: none
  auth: none
}

params:path {
  id: replace-with-user-id
}

docs {
  Requires an active session cookie — run "Sign In" first in the same run.
}
