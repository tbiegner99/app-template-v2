-- seed.sql
-- Placeholder for a future database dump.
-- Replace this file with a full pg_dump output to pre-populate the local database.
-- The reset-db command (__SLUG__ reset-db) applies this file after recreating the database.

-- Auth note: SuperTokens creates its own schema tables (supertokens_*) automatically on first boot.
-- Application user rows in the `users` table are created by the backend on signup.
-- To create the default local-dev admin user, run: local-dev/scripts/cmd/seed-auth.sh
