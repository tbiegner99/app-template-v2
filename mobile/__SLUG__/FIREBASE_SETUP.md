# Firebase setup required

This app expects Firebase project files that are intentionally NOT included
in the template (they contain real per-project credentials):

- `android/app/google-services.json` — download from Firebase Console
- `ios/Runner/GoogleService-Info.plist` — download from Firebase Console

Add your own after creating a Firebase project for this app.
